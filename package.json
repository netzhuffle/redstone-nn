{
  "name": "redstone-nn",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "format": "prettier . --write",
    "verify-types": "tsc",
    "checks": "bun run lint && bun run format && bun run verify-types && bun test"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.7.0",
    "@typescript-eslint/parser": "^6.7.0",
    "bun-types": "latest",
    "eslint": "^8.49.0",
    "eslint-config-prettier": "^9.0.0",
    "prettier": "^3.0.3",
    "typescript": "^5.2.2"
  }
}
